<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Leitura do sensor corporal (frequência cardíaca) -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.BODY_SENSORS_BACKGROUND" />

    <!-- Obrigatório no Android 12+ para sensores de atividade física -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Health Connect (dispositivos que exigem camada adicional) -->
    <uses-permission
        android:name="android.permission.health.READ_HEART_RATE"
        tools:ignore="HealthConnectPolicy" />

    <!-- Mantém CPU ativa durante leitura do sensor -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Feedback tátil de alarme -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Hardware obrigatório: relógio com sensor cardíaco -->
    <uses-feature android:name="android.hardware.type.watch" />
    <uses-feature
        android:name="android.hardware.sensor.heart_rate"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <!-- Não depende de app companion no celular -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/MainActivityTheme.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
